#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int a = 5;
    int b = 7;
    int c = 2;
    int d = 9;
    printf("The sum of %d and %d is %d\n", a, b, a + b);
    printf("The product of %d and %d is %d\n", c, d, c * d);
    return 0;
}